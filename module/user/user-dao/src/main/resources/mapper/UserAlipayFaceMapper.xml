<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fqgj.youjie.user.dao.impl.UserAlipayFaceDaoImpl">

    <select id="selectByUserId" parameterType="Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_alipay_face
        WHERE user_id = #{userId,jdbcType=BIGINT} AND deleted=0
        ORDER BY gmt_create DESC limit 1
    </select>

    <select id="selectByBizNo" parameterType="String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_alipay_face
        WHERE biz_no = #{bizNo,jdbcType=VARCHAR} AND deleted=0
        ORDER BY gmt_create DESC limit 1
    </select>

    <update id="deleteByUserId">
        UPDATE  user_alipay_face
        SET deleted = 1
        WHERE user_id = #{userId,jdbcType=BIGINT}
    </update>

</mapper>