<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fqgj.youjie.user.dao.impl.UserFriendDaoImpl">

    <select id="selectFriendIdsByUserId" parameterType="Long" resultType="Long">
        SELECT friend_id
        FROM user_friend
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND deleted=0
        ORDER BY `gmt_modified` DESC
        LIMIT 50
    </select>

    <select id="selectByUserIdAndFriendId" parameterType="HashMap" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_friend
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND friend_id = #{friendId,jdbcType=BIGINT}
        AND deleted=0
    </select>

    <select id="countByUserIdAndFriendId" parameterType="HashMap" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM user_friend
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND friend_id = #{friendId,jdbcType=BIGINT}
        AND deleted=0
    </select>

    <update id="deleteByUserIdAndFriendId">
        update  user_friend
        set deleted = 1
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND friend_id = #{friendId,jdbcType=BIGINT}
        AND deleted=0
    </update>

</mapper>